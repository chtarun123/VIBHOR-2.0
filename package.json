{
  "name": "vibhor",
  "version": "5.0.0",
  "private": true,
  "description": "VIBHOR — Indian Heritage Ecosystem (final build: Express + SQLite backend, auth with guest mode, no fake data)",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "seed": "node server/db.js --reseed",
    "check": "node --check server/server.js && node --check server/db.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "multer": "^1.4.5-lts.2"
  }
}
